<div class="container">
  <div class="row">
    <div class="headingTitles mx-auto py-4">
      <h1>Cohorts Database</h1>
    </div>
  </div>
  <div class="row mx-auto">
    <div class="col-lg-12 mx-auto">
      <div>
        <%= link_to 'View Student Database', students_path, class:'MainNav' %>
        |
        <%= link_to 'View Instructor Database', instructors_path, class:'MainNav' %>
        |
        <%= link_to 'View Courses Database', courses_path, class:'MainNav' %>
        |
        <%= link_to 'View Cohorts Database', cohorts_path, class:'MainNav' %>
        |
        <%= link_to 'View Current Cohorts', active_cohorts_path, class:'MainNav' %>
      </div>
    </div>
  </div>
  <div class="row mx-auto text-center">
    <div class="col-lg-12 text-center mx-auto">
      <%= link_to 'Create A New Cohort', new_cohort_path, class:'btn addBtns' %>
    </div>
  </div>

  <div class="row mx-auto">
    <div class="col-lg-6  mx-auto py-2">
      <% @cohorts.each do |cohort| %>

        <div class="py-1 text-left main-Cohorts">
          <%= cohort.name %>
          <br>
          <%= link_to 'View Cohort Information', cohort_path(cohort), class:'UserLinks btn' %>
          |
          <%= link_to 'Edit Cohort', edit_cohort_path(cohort), class:'UserLinks btn' %>
          |
          <%= link_to 'Delete Cohort', cohort, class:'deleteCohort UserLinks btn' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
